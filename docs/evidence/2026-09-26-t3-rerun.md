# T3 live-gate rerun, 2026-09-26

Actor type: `automated-real`

## System under test

- Agent Tag commit: `52beed7`
- T3 Code: `0.0.42`, commit `719a76ca1dbf5490f1aa33ffb9966301e02be9a9`
- Platform: macOS `26.6.2`, arm64
- Bun: `1.3.13`
- Transport: restricted service token over loopback HTTP and authenticated WebSocket RPC

## Result

The T3 pin check passed and the restarted loopback server returned HTTP `200`. The complete live integration file ran seven cases in 48.40 seconds. Six passed:

1. Release probe and authenticated provider capability projection.
2. Stable-command receipt replay.
3. A real supervised Codex `gpt-5.6-sol` turn and completed snapshot reconciliation.
4. A real Codex command approval and completed turn.
5. Durable coordinator replay after SQLite close/reopen and worker replacement.
6. Command rejection followed by interruption of a waiting turn.

The explicit Claude `claudeAgent` plus `claude-sonnet-4-6` case failed after T3 entered its error state. The committed test reported only the sanitized `provider-api-errors` classification. No token, private message, or raw T3 log was captured.

This rerun confirms that the Slack setup changes did not regress the pinned T3 path. It does not change the provider matrix: Codex is executable, while Claude remains failed until a real selected turn completes.
