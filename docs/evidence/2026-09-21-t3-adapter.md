# T3 adapter evidence, 2026-09-21

Actor type: `automated-real`

## System under test

- Agent Tag branch: `feat/t3-slack-spike`
- T3 Code: `0.0.42`, commit `719a76ca1dbf5490f1aa33ffb9966301e02be9a9`
- T3 release archive SHA-256: `f15b8c4c47092b3d3a4ae2c24b1e1bb93d24a618edbc8aedfc4455a36b76774a`
- Platform: macOS `26.6.2`, arm64
- Bun: `1.3.13`
- Transport: loopback HTTP authentication plus Effect RPC over authenticated WebSocket

## Checks run

`bun run enroll:t3` used the setup credential once to mint a separate service token. The resulting session reported exactly `orchestration:read` and `orchestration:operate`. A request to `/api/auth/clients` with that token returned HTTP `403`. The secret file and its parent reported modes `0600` and `0700`. No credential is stored in the repository or evidence. The headless bootstrap code in the server startup log was checked after enrollment and reported `authenticated: false`.

`bun run test:t3` ran against the release binary. Six required cases passed:

1. The adapter minted a WebSocket ticket, completed the Effect RPC handshake, called `server.probe`, decoded `server.getConfig`, and projected provider/model capability data.
2. The adapter sent the same `project.create` command twice with one stable command ID. T3 returned the same receipt sequence both times. The test then deleted the T3 project and removed its temporary directory.
3. The adapter created an isolated Git fixture, selected `codex` with `gpt-5.6-sol` in `approval-required` mode, completed a tool-free provider turn, replayed thread events to the synchronization marker, and reconciled the authenticated HTTP snapshot. The snapshot held the explicit provider/model, a completed latest turn, and the exact non-streaming assistant reply.
4. A second supervised turn requested command approval before reading its fixture. The test responded to each distinct request with one-shot `accept`, observed the requests resolve, received the exact file content, and removed the temporary project and repository.
5. A durable SQLite operation flowed through the production coordinator. After T3 accepted the turn, a forced bridge failure returned the operation to its durable pending state. The test closed and reopened SQLite, constructed a new worker, replayed the stable IDs, reconciled the completed snapshot, and atomically committed the operation plus its exact Slack outbox reply.
6. A supervised command was declined and the turn settled with no pending approval. A separate turn was interrupted while waiting for approval and reached the distinct `interrupted` state.

The catalog reported these real providers ready and authenticated:

| Instance | Required model observed |
| --- | --- |
| `codex` | `gpt-5.6-sol` |
| `claudeAgent` | `claude-opus-5` |

## Provider failure found

The additional Claude execution gate failed. T3 accepted explicit `claudeAgent` selections for `claude-opus-5`, its catalog-default `claude-fable-5-1`, and the stable `claude-sonnet-4-6`, but each turn entered `error`; the parsed session diagnostic classified the cause as repeated provider API errors. No raw T3 log was read. The committed live gate uses `claude-sonnet-4-6` and remains red until a real turn completes with a sticky provider/model snapshot.

The catalog state therefore does not prove executable provider readiness. Agent Tag now rejects missing, disabled, unauthenticated, non-ready, and missing-model configurations before Slack startup, but only a real turn can satisfy provider compatibility.

The pinned release source maps recognized Claude authentication failures to a signed-out diagnostic and rejected rate-limit windows to a usage-limit diagnostic before its terminal result. The observed generic `api_error` is the fallback after SDK retries when neither hint was captured. Since three catalog models produced the same fallback, the exposed boundary rules out T3's recognized authentication/rate-limit paths and a single model mapping, but cannot distinguish upstream API availability from account entitlement. That unresolved user/provider boundary remains the blocker.

This evidence proves release compatibility, least-privilege authentication, provider catalog projection, T3 command deduplication, a completed real Codex turn, event/snapshot reconciliation, real approval/decline/interruption control, and coordinator reconciliation after a store/worker restart. It also proves the configured Claude runtime is currently failing despite a ready catalog state. It does not yet prove OS process-kill recovery, T3 server restart, provider capability parity, or live Slack behavior.
